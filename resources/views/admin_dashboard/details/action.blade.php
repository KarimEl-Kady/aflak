<a class="btn requests" href="{{ route('details.requestts.index', $id) }}">
    {{__('messages.show_requests')}}

</a>
<a class="btn offer" href="{{ route('details.ofers.index', $id) }}">
    {{__('messages.show_offers')}}
</a>
